# 与nacos配置文件的信息要一致
spring:
  application:
    name: userservice
  profiles:
    active: prod   #当前环境
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yaml   #nacos在线配置文件后缀名